# Basic Oracle SQL operations through TNS: DDL, DML, and SELECT queries.

exec
CREATE TABLE lt_basic (id NUMBER, name VARCHAR2(100))
----

exec
INSERT INTO lt_basic (id, name) VALUES (1, 'alice')
----

exec
INSERT INTO lt_basic (id, name) VALUES (2, 'bob')
----

exec
INSERT INTO lt_basic (id, name) VALUES (3, 'carol')
----

query rowsort
SELECT id, name FROM lt_basic
----
ID NAME
1 alice
2 bob
3 carol

query
SELECT id, name FROM lt_basic WHERE id = 2
----
ID NAME
2 bob

query
SELECT id, name FROM lt_basic ORDER BY id DESC
----
ID NAME
3 carol
2 bob
1 alice

exec
UPDATE lt_basic SET name = 'alice2' WHERE id = 1
----

query
SELECT name FROM lt_basic WHERE id = 1
----
NAME
alice2

exec
DELETE FROM lt_basic WHERE id = 3
----

query rowsort
SELECT id, name FROM lt_basic
----
ID NAME
1 alice2
2 bob

exec
DROP TABLE lt_basic
----
