# Unsupported CQL types.
#
# Types recognized by the parser but not supported by the translator
# produce "unsupported CQL type" errors. Types not in the CQL
# specification at all produce "expected data type" errors.
#
# Previously unsupported types (smallint, tinyint, date, time, duration)
# are now supported — see types_integer and types_timestamp for tests.

exec
USE defaultdb
----
